```@meta
CollapsedDocStrings = true
```

# XML utilities

Internal LightXML navigation helpers used to read PhysiCell output XML. These are the
read-side subset of ModelManager's XML utilities, copied here so PhysiCellOutput does not
depend on ModelManager. They are not exported.

```@autodocs
Modules = [PhysiCellOutput]
Pages = ["xml_utilities.jl"]
```
